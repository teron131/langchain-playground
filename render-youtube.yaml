services:
  - type: web
    name: youtube-summarizer-ui
    runtime: python
    region: singapore
    plan: starter
    buildCommand: "pip install --upgrade pip && pip install -r requirements-youtube.txt"
    startCommand: "uvicorn api.youtube_with_ui:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: GEMINI_API_KEY
        sync: false
      - key: FAL_KEY
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false